#
# swis-api (swapi) / environment constatns
#

APP_NAME=swis-api
APP_ROOT=/opt/${APP_NAME}
APP_URL=swapi.savla.su
APP_URLS_TRAEFIK=`${APP_URL}`,`localhost4`,`localhost`
APP_VERSION=5.4.19


#
# build constants
#

ALPINE_VERSION=3.17
GOLANG_VERSION=1.20.4

# note: prod image needs 'tzdata' package present
TZ=Europe/Vienna

DOCKER_INTERNAL_PORT=8050
DOCKER_EXTERNAL_PORT=8050
DOCKER_CONTAINER_NAME=${APP_NAME}-run
DOCKER_IMAGE_TAG=${APP_NAME}:${APP_VERSION}-go${GOLANG_VERSION}
DOCKER_NETWORK_NAME=traefik_hermes
DOCKER_USER=swapi

DOCKER_DEV_CONTAINER=${APP_NAME}-dev
DOCKER_DEV_PORT=8051


#
# runtime constants
#

# gin server modes: 'debug', 'release' (=production, less verbose logging, no panics or loaded handlers info)
GIN_MODE=debug

APP_FLAGS=
DUMP_DIR=/mnt/backup/swis-api/
ROOT_TOKEN=fd4422301ss11DE222l---change-me
GOMAXPROCS=1

